package circularQueuePractice;

public class Notes {
	// Dequeue: Just incremenet head. The queue only contains values starting from
	// head and ending at tail.
	// When the queue is empty, head and tail will be set to -1
}
